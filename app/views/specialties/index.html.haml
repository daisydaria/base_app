%h1 Специальности

%table.table.table-bordered
  %thead
    %tr
      %th Название
      %th Код
      %th Поколение
      %th Уровень
      -if is_operator?
        %th Действия

  %tbody
    -@specialties.each do |specialty|
      %tr
        %td= specialty.title
        %td= specialty.code 
        %td= specialty.gen
        %td= specialty.level
        -if is_operator?
          %td
            = link_to edit_specialty_path(specialty), class: "btn btn-warning btn-xs" do
              =fa_icon "edit"
              Редактировать
            = link_to specialty_path(specialty), :method => :delete, :data => { :confirm => 'Вы уверены?' }, class: "btn btn-danger btn-xs" do
              =fa_icon "remove"
              Удалить


-if is_operator?
  = link_to 'Новая специальность', new_specialty_path, "data-toggle" => "tooltip", :title => "Создать специальность", class: "btn btn-primary btn-xs"